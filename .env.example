# Base de datos
DATABASE_URL="mysql://usuario:contraseña@host:3306/nombre_db"

# NextAuth
# Producción: usar la IP o dominio real sin trailing slash
# Generar secret con: openssl rand -base64 32
NEXTAUTH_SECRET="genera_uno_con: openssl rand -base64 32"
NEXTAUTH_URL="https://vec.atmosfera-soltec.com"
